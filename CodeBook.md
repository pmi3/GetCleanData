Code BookThis document describes the code inside run_analysis.R.The code is splitted (by comments) in some sections:* Helper functions* Constants* Downloading and loading data* Manipulating data* Writing final data to CSVHelper functionsSome functions to avoid code repetition and make the rest of code more clean.UnzipThis first creates target directory;  does the download of zip; extracts the files in separate directories; it also renames the named directory to UciHarMergerReads six columns of data, doing col bind and row bind accordingly to a data.frame. Columns are renamed slightly to help in easy join, using merge(). “Mean” and “std” column names are searched in the features file and subsetted into a new data frame.Then aggregated and grouped by activity.